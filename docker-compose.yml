version: "2"

services:

  api:
    build:
      context: .
      dockerfile: src/deploy/api/Dockerfile
    command: /bin/sh /bin/dev
    volumes:
      - ./src/deploy/api/config.toml:/etc/fetch.toml:ro
      - ./target/debug:/opt
      - ./src/deploy/api/dev.sh:/bin/dev:ro
    links:
      - db
    depends_on:
      - db
      - migrate
    environment:
      DATABASE_URL: postgres://postgres@db/postgres

  web:
    build:
      context: .
      dockerfile: src/deploy/web/Dockerfile
    volumes:
      - ./src/deploy/api/nginx.conf:/etc/nginx/service.d/api.conf:ro
      - ./public:/www:ro
    ports:
      - 3000:80
    links:
      - api
    depends_on:
      - api

  migrate:
    build:
      context: .
      dockerfile: src/deploy/migrate/Dockerfile
    restart: on-failure
    depends_on:
      - db
    links:
      - db
    environment:
      DATABASE_URL: postgres://postgres@db/postgres
      
  db:
    image: postgres:10-alpine
    ports:
      - 5432:5432
    volumes:
      - /var/lib/postgresql/data

import * as React from "react";
import SeriesForm from "../components/seriesForm";

export default () => (
        <div className="container box">
            <SeriesForm back="/" />
        </div>);
